import { Router } from "express";

const router = Router();

router.post("/", (req, res) => {});

router.put("/", (req, res) => {});

router.get("/:id", (req, res) => {});

router.get("/bulk", (req, res) => {});

export default router;
